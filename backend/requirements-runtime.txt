# Minimal dependency set for running the FastAPI app in production/deployment.
#
# requirements.txt additionally includes rasterio/geopandas/rasterstats/numpy/
# scipy, which are only used by backend/scripts/preprocess_flood_data.py (a
# one-off offline script that regenerates somalia_basins_ffsi.geojson — that
# output file is already committed and loaded directly at runtime by
# flood_processor.py). Deploying with only this file avoids compiling/
# installing the heavier GDAL/GEOS/PROJ-backed geo stack, which the running
# API never imports.
earthengine-api~=1.7
google-auth~=2.47
python-dateutil>=2.9,<3
fastapi~=0.128
uvicorn~=0.40
openai>=1.0,<2
python-dotenv>=1.0,<2
